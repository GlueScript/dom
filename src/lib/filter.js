/*
 * extract items from doc (a dom html/xml document) using filter parameter
 * return an array of 'items' 
 * allow filtering for elements or attributes
 */
exports.extract = function(doc, filter) {
    console.log('filter: ' + doc + ' : ' + filter);
    return [['one thing']];
};
