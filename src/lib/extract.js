/*
 * filter doc (a dom (html/xml) document using filter parameter
 * return an array of 'items' 
 * allow filtering for elements or attributes
 */
exports.filter = function(doc, filter) {
    console.log('filter: ' + doc + ' : ' + filter);
    return [['one thing']];
};
